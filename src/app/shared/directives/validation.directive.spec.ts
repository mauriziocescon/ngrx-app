import { BlockValidationDirective } from "./validation.directive";

describe("BlockValidationDirective", () => {
  it("should create an instance", () => {
    const directive = new BlockValidationDirective();
    expect(directive).toBeTruthy();
  });
});
